<!DOCTYPE html>
<html>
  <head>
    <title>Add user</title>
    <meta charset="utf-8" />
  </head>
  <body>
    {% block content %}
    <div>
      <h3>Add user</h3>
      <form method="post">
        Username: <input name="username" id="username" value="{{ request.form['username'] }}" /><br />
        Password: <input name="password" id="password" value="{{ request.form['password'] }}" /><br />
        Full name: <input name="full_name" id="full_name" value="{{ request.form['fullname'] }}" /><br />
        <input type="submit" value="Submit" />
      </form>
    </div>
    {% endblock %}
  </body>
</html>

