<!DOCTYPE html>
<html>
  <head>
    <title>Edit {{ userinfo[0] }}</title>
    <meta charset="utf-8" />
  </head>
  <body>
    {% block content %}
    <div>
      <form>
	Username: <input name="username" value="{{ userinfo[0] }}" /><br />
	Password: <input name="password" value="{{ userinfo[1] }}" /><br />
	Full name: <input name="full_name" value="{{ userinfo[2] }}" /><br />
	<input type="submit" value="Submit" />
      </form>
    </div>
    {% endblock %}
  </body>
</html>
